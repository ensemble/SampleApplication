<h1>Shopping cart</h1>

<ul>
	<? foreach($basket->getItems() as $item):?>
		<li><?= $item->getProduct()->getTitle()?> (<?= $item->getQuantity()?>)</li>
	<? endforeach?>
</ul>

<? if(count($basket->getItems())):?>
	<form method="post" action="<?= $this->url('/cart/clear')?>">
		<button type="submot">Clear basket</button>
	</form>
<? endif?>